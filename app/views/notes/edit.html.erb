<h1>Modificar Nota</h1>

<%= form_for @note do |f| %>
  <div>
    <%= f.label :content, "Contenido:" %><br>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit "Modifiar" %>
  </div>
<% end %>